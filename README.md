# CS2430-Group1-Project2
This program does contain a library that will need to have its build path changed.
In eclipse this done by right clicking the project -> properties -> Java Build Path -> Libraries and changing the commons-collections4-4.5.0.jar's path to the jar file contained in the lib folder where you downloaded this program.
